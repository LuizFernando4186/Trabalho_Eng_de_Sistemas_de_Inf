<%= form_with(model: alternativa) do |form| %>
  <% if alternativa.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(alternativa.errors.count, "error") %> prohibited this alternativa from being saved:</h2>

      <ul>
        <% alternativa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :alternativa, style: "display: block" %>
    <%= form.text_area :alternativa %>
  </div>

  <div>
    <%= form.label :correta, style: "display: block" %>
    <%= form.check_box :correta %>
  </div>

  <div>
    <%= form.label :questao_id, style: "display: block" %>
    <%= form.text_field :questao_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
