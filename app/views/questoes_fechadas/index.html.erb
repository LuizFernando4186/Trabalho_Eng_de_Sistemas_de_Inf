<p style="color: green"><%= notice %></p>

<h1>Questões Fechadas</h1>

<div id="questoes_fechadas">
  <% @questoes_fechadas.each do |questao_fechada| %>
    <%= render questao_fechada %>
    <p>
      <%= link_to "Ver questão", questao_fechada %>
    </p>
  <% end %>
</div>

<%= link_to "Nova questão", new_questao_fechada_path %>
